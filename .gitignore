# Source renders dropped here for conversion. The web-sized WebP versions in
# official/ are what ships; the originals are 1-15MB each and have no business
# in a public repo.
incoming/

# Scratch state for _gen.py; the generated pages under products/ are committed.
_products.json
